class CreateLangMappings < ActiveRecord::Migration[<%= ActiveRecord::Migration.current_version.to_s %>]
  def self.up
    create_table :active_admin_comments do |t|
      t.string :field
      t.references :kclass, polymorphic: true
      t.string :en
      t.string :zh-cn

      t.timestamps
    end
    add_index :active_admin_comments, [:namespace]
  end

  def self.down
    drop_table :active_admin_comments
  end
end